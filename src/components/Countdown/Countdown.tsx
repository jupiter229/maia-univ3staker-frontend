// import { useCountdownTimer } from "@/utils/CountdownTimer";
// import PropTypes from 'prop-types';

// type Props = {
//   title: typeof PropTypes.string;
//   timeTarget: typeof PropTypes.object;
// };

// export const ViewCountdown: React.FC<Props> = ( props ) => {

//   const CountDown = (timeTarget : Date) => {
//     const secondsLeft = useCountdownTimer(timeTarget);
//   }

//   return (
//     title === "Upcoming" && ( <>Upcoming in</> <></> )
//   );
// };
